# ⚙️ **2D Physics Simulation To-Do List**

---

## **Level 1: Core Motion**

* [x] Gravity as a constant (`f32`), Y-axis only.
* [x] Particle component with velocity.
* [x] Basic Euler integration: velocity → position.
* [x] Floor collision + bounce.
* [x] Random initial velocities on spawn.
* [ ] Add right/left walls if using X-axis motion.

---

## **Level 2: Better Physics**

* [ ] Add **friction/damping** on bounce to stop infinite motion (`velocity *= 0.8`).
* [ ] Add a **restitution coefficient** (controls how bouncy the particle is).
* [ ] Add user-spawned particles on mouse click.
* [ ] Add wind force (constant or toggleable).
* [ ] Add gravity switcher (up/down, stronger/weaker).
* [ ] Optional: Make gravity a Resource.

---

## **Level 3: Multi-Particle Forces**

* [ ] Add basic **attraction/repulsion** between particles (gravity or electric style).
* [ ] Add mass to particles and calculate acceleration = force / mass.
* [ ] Limit max velocity to avoid simulation blowup.
* [ ] Optional: collision between particles (circle hitbox, basic overlap resolution).

---

## **Level 4: Visual Polish + Complexity**

* [ ] Color particles based on their velocity (fast = red, slow = blue).
* [ ] Trail effects (optional, cosmetic).
* [ ] Boundary bounce smoothing (prevent them from sticking at borders).
* [ ] Basic UI to reset particles, spawn many, toggle forces.
* [ ] Pause / play simulation.

---

## **Boss Mode (Optional)**

* [ ] Switch to Verlet integration or semi-implicit Euler for better stability.
* [ ] Add spring forces (`F = -k * x`) between particles.
* [ ] Add pressure (simulate fake gas clouds).
* [ ] Optimize using bevy’s spatial queries (Quadtree later if needed).

